# This project shows how to make a web server with an ESP8266 NodeMCU board that displays a web page with multiple sliders. Communication between clients and the ESP8266 is done using the WebSocket protocol. 
